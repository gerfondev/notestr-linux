[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nostr-private-notes"
dynamic = ["version"]
description = "Notes privées Nostr pour Linux, compatibles avec les documents personnels Pages"
requires-python = ">=3.10"
dependencies = ["monstr==0.1.9", "websockets>=14,<18", "keyring>=24,<26", "markdown-it-py>=3,<5", "argon2-cffi>=23,<26", "weasyprint>=70,<71"]

[project.optional-dependencies]
test = ["pytest>=8,<10"]

[project.scripts]
nostr-notes = "nostr_notes.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.setuptools.package-data]
nostr_notes = ["assets/*"]

[tool.setuptools.dynamic]
version = {attr = "nostr_notes.__version__"}
